name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
    
    - name: Install system dependencies
      run: sudo apt-get update && sudo apt-get install -y ffmpeg
    
    - name: Install Python dependencies
      run: pip install -r requirements.txt pytest
    
    - name: Run tests
      env:
        PYTHONPATH: ${{ github.workspace }}
      # capture/ and pult/ are hermetic on a runner with no sound card: the real-hardware tests
      # in capture/ SKIP (honest absence) rather than pass, and everything else uses fakes. A
      # suite that is written but never run is a suite that can never fail.
      run: pytest tests/ .github/test_verify_macos_min.py cutter/test_normalize_loudness.py automixer/test_utils.py automixer/test_uxn_stream.py capture/ pult/ -v
